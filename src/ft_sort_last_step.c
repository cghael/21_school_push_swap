//
// Created by Anton on 17.05.2020.
//

#include "push_swap.h"
/*
void		ft_sort_last_step(t_order **cmd, t_elem **src, int step)
{
	if (((*src)->name == 'a' && (*src)->index > (*src)->next->index) \
		|| ((*src)->name == 'b' && (*src)->index < (*src)->next->index))
		ft_sa_sb(cmd, src, (*src)->name);
	else if (step == 0 && STACK_NOT_SORTED == ft_check_stack_sorted(*src, (*src)->qnty))
	{
		ft_ra_rb(cmd, src, (*src)->name);
		step++;
	}
	else if (step == 1)
	{
		ft_rra_rrb(cmd, src, (*src)->name);
		step++;
	}
	else
		return ;
	ft_sort_last_step(cmd, src, step);
}
 */